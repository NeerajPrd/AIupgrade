services:
  app:
    image: ghcr.io/neerajprd/aiupgrade:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      LITE_MODE: "true"
      DATA_DIR: /data
      DATABASE_URL: postgresql+asyncpg://fagoon:fagoon@db:5432/fagoon
      WEB_CONCURRENCY: "1"
      OLLAMA_BASE_URL: "${OLLAMA_BASE_URL:-}"
      EVOLUTION_API_KEY: "${EVOLUTION_API_KEY:-}"
      EVOLUTION_API_URL: "http://evolution-api:8080"
      WEBHOOK_URL: "${WEBHOOK_URL:-http://app:8000}"
    volumes:
      - ${FAGOON_DATA_DIR:-~/.fagoon}:/data
    depends_on:
      db:
        condition: service_healthy
      evolution-api:
        condition: service_started

  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: fagoon
      POSTGRES_PASSWORD: fagoon
      POSTGRES_DB: fagoon
    volumes:
      - fagoon_db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fagoon"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ---- WhatsApp Gateway (Evolution API) ----
  evolution-api:
    image: evoapicloud/evolution-api:v2.3.2
    environment:
      - AUTHENTICATION_TYPE=apikey
      - AUTHENTICATION_API_KEY=${EVOLUTION_API_KEY}
      - WPP_LID_MODE=false
      - DATABASE_PROVIDER=postgresql
      - DATABASE_CONNECTION_URI=postgresql://evolution:secretpassword@evolution-db:5432/evolution?schema=public
      - DATABASE_CONNECTION_CLIENT_NAME=evolution_api
      - CACHE_REDIS_ENABLED=true
      - CACHE_REDIS_URI=redis://evolution-redis:6379
      - WEBSOCKET_ENABLED=false
      - RABBITMQ_ENABLED=false
      - SQS_ENABLED=false
    volumes:
      - evolution_instances:/Evolution/instances
    depends_on:
      - evolution-db
      - evolution-redis
    restart: unless-stopped

  evolution-db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=evolution
      - POSTGRES_PASSWORD=secretpassword
      - POSTGRES_DB=evolution
    volumes:
      - evolution_db_data:/var/lib/postgresql/data
    restart: unless-stopped

  evolution-redis:
    image: redis:alpine
    restart: unless-stopped
    volumes:
      - evolution_redis_data:/data

  ollama:
    image: ollama/ollama:latest
    profiles: ["ollama"]
    restart: unless-stopped
    ports: ["11434:11434"]
    volumes:
      - fagoon_ollama:/root/.ollama

volumes:
  fagoon_data:
  fagoon_db:
  fagoon_ollama:
  evolution_instances:
  evolution_db_data:
  evolution_redis_data:
